# 미결 사항

원 스펙 §10 항목 + 조사 중 새로 생긴 것들.
**🔴 는 코드 작성 전 답이 필요하고, 🟡 는 진행하면서 채워도 된다.**

조사로 이미 해소된 항목은 §2에 근거와 함께 정리했다.

---

## 1. 답이 필요한 것

### 🔴 Q-01 — 생성한 hwp가 한글에서 정상적으로 열리는가

**가장 중요하고, 가장 싸게 확인할 수 있는 항목.**

조사 중 실제로 셀 텍스트를 바꿔 재생성한 파일을 만들어 두었다.
Windows 한글에서 열어 확인만 해 주면 된다.

- 확인 대상: `fixtures/verify-write-test.hwp`
- 원본 `1-1` 행의 `제10차 인사위원회` 자리에
  `AI데이터팀 주간회의 및 시스템 점검 (자동병합 테스트)` 가 들어가 있으면 성공
- 표 테두리·열 너비·글꼴이 그대로인지 함께 봐 주세요

**이 한 번의 확인으로 두 가지가 동시에 판정된다.**

| 확인되는 것 | 내용 |
|---|---|
| 레코드 수술 | 셀 텍스트 치환 + `nChars` 플래그 처리가 옳은가 |
| `cfb` 센티널 | 쓰기 라이브러리가 넣는 `\x01Sh33tJ5` 스트림을 한글이 무시하는가 |

두 번째는 조사 중 발견한 것이다. `cfb`가 출력 파일에 4바이트 표식 스트림을
**무조건 추가**하며 제거할 수 없다 (원본 9개 → 출력 10개 스트림).
MS Office는 같은 것을 무시하므로 한글도 그럴 가능성이 높지만, 확인이 필요하다.
이 파일에 이미 포함돼 있으므로 **따로 시험할 것은 없다.**
상세: [R-001 §7.1](docs/research/001-hwp-format-findings.md), [HM-08a](docs/spec/08-hwp-merge-engine.md)

> 여기서 실패하면 [ADR-0001](docs/adr/0001-hwp5-binary-record-engine.md)을 되돌리고
> `.hwpx` 전환(C안)으로 가야 한다. **Phase 2 착수 전에 반드시.**
> Phase 1에는 영향 없음.

---

### 🔴 Q-02 — 8명의 이름 · 이메일 · 팀

시드 데이터에 필요하다. Phase 1 착수의 유일한 하드 블로커.

| 이름 | KEI 이메일 | 팀 | 관리자 |
|---|---|---|---|
| 최명헌 | ? | AI | ✔ |
| 장혜정 | ? | 홍보? | |
| ? | ? | | |

- 팀 값은 `AI` / `홍보` / `시스템` / `도서관` 중 하나 (스펙 §3 순서)
- 이메일은 **Cloudflare Access 정책에 넣을 것과 정확히 같아야** 한다 (AU-04)
- 팀 배정은 Phase 2 병합 정렬용이라 **Phase 1에서는 비워도 동작**한다 (DM-09).
  이름 + 이메일만 있으면 시작 가능

---

### 🟡 Q-03 — 8명이 모두 KEI 계정으로 Access 로그인이 가능한가

스펙 §5는 MAC 등록 제약 우회가 목적이라고 했다.

- 현재 Access 정책의 인증 방식은? (Google SSO / One-time PIN / 기타)
- 8명 전원이 그 방식으로 로그인 가능한가
- 사내 PC에서 `excusa.uk` 접근이 방화벽에 막히지는 않는가

> 도입 전에 **1명으로 먼저 시험**해 보는 것을 권한다.
> 8명에게 안내한 뒤 로그인이 안 되면 신뢰를 잃는다.

---

### 🟡 Q-04 — Access 애플리케이션 AUD 태그

`CF_ACCESS_AUD` 환경변수에 필요하다 (AU-02).
Access 앱을 만든 뒤 Zero Trust 대시보드에서 복사해 주면 된다.

Access 앱 생성 전에는 존재하지 않으므로, **배포 직전에 확보**하면 된다.

---

### 🟡 Q-05 — `.hwpx` 제출을 허용할 것인가

현재 스펙은 **받아 둔다**로 잡았다 (ST-05).

- 한글 버전에 따라 기본 저장 형식이 `.hwpx`인 경우가 있다
- 거부하면 사용자가 이유를 모른 채 막힌다
- 다만 Phase 2 병합에서 `.hwpx` 읽기를 따로 구현해야 한다 (HM-23)

**대안**: `.hwpx`를 받되 업로드 화면에서 `.hwp 권장` 안내를 띄운다.

→ 8명에게 "한글에서 저장할 때 형식을 hwp로" 한 번만 안내하면 해결될 수도 있다.
   현장 상황을 아는 쪽이 판단해 주세요.

---

### 🟡 Q-06 — 마감 정각(14:00:00)은 포함인가

현재 스펙: **포함** (`now > deadline`일 때만 잠금, WS-T14).

14:00:00.000에 도착한 요청을 받아준다. 반대로 하려면 `>=`로 바꾸면 된다.
실무상 차이는 거의 없지만 테스트에 명시해 두려고 확인한다.

---

### 🟡 Q-07 — 터널 토큰 노출 범위

`systemctl status cloudflared` 출력에 터널 토큰이 평문으로 보인다.
이 서버에 셸 접근이 가능한 사람이 여럿이면 검토가 필요하다.

- 이 프로젝트가 만든 문제는 아니다 (기존 설정)
- 다만 `worklog`를 같은 터널에 붙이므로 노출 범위가 넓어진다
- 필요하면 systemd `EnvironmentFile=`(권한 0600)로 옮기는 것을 권한다

> 이 서버의 셸 접근 인원이 Sean뿐이면 조치 불필요.

---

### 🟡 Q-08 — 서브도메인 이름

현재 가정: `worklog.excusa.uk`

다른 이름을 원하면 알려주세요 (`weekly`, `report`, `wr` 등).
Access 앱 도메인과 일치해야 한다.

---

### 🟡 Q-09 — 배포 시점 · 첫 적용 주차

- 언제부터 실제 운영에 쓸 것인가
- 첫 주는 **이메일 병행**을 권한다 (안 되면 즉시 되돌릴 수 있게, OPS-18)
- 과거 주차는 소급 생성하지 않는다 (WS-12)

---

### 🟡 Q-10 — Phase 3 리마인드가 정말 필요한가

관리자 화면에 **미제출자 이름 복사 버튼**을 넣기로 했다 (PG-27).

```
[ 미제출 3명 복사 ]  →  "장혜정, 홍길동, 김철수"
```

이걸 사내 메신저에 붙여넣으면 Phase 3의 효용 대부분이 나온다.
자동 알림 연동(웹훅) 없이 이걸로 충분한지, Phase 1 운영해 보고 판단하는 것을 제안한다.

---

## 2. 조사로 해소된 항목 ✅

원 스펙 §10의 4개 중 3개가 실측으로 해결됐다.

### ✅ 확장자는 `.hwp`인가 `.hwpx`인가 → **`.hwp` (HWP 5.0 바이너리)**

```
$ file MWreports/*.hwp
8월_1주차_…AI홍보전략실.hwp: Hancom HWP file, version 5.0
8월_2주차_…AI홍보전략실.hwp: Hancom HWP file, version 5.0
_draft_M월_W주차_…hwp:      Hancom HWP file, version 5.0
```

`_AIDT/*.hwpx` 40여 개는 **구버전(AI데이터팀)** 이며 사용자 지시대로 범위 외.
참고로 검사 결과 **표를 하나도 포함하지 않는 불릿 문서**였다 → 재사용 가치 없음.

→ 스펙 §6의 "`.hwpx`면 순수 파이썬 가능" 경로는 **성립하지 않는다.**
   [ADR-0001](docs/adr/0001-hwp5-binary-record-engine.md) 참조.

---

### ✅ 표에 누름틀(필드)이 있는가 → **없다**

전 셀이 일반 문단 텍스트다. 필드 컨트롤 레코드가 하나도 없다.

→ 스펙 §6의 `put_field_text()` 방안은 적용 불가.
→ 대신 **셀 주소 기반 직접 치환**을 쓴다 (HM-09).

부수 확인: **병합 셀도 없다.** 전 셀 `(colSpan, rowSpan) = (1,1)`, 균일 5열.
스펙 §1이 우려한 "병합 셀" 문제는 실제로 존재하지 않는다. 병합 엔진이 상당히 단순해진다.

---

### ✅ "N주차" 규칙이 조직 관례와 맞는가 → **맞다**

`8월_2주차_…hwp` 내용에서 역산했다.

| 항목 | 일자 |
|---|---|
| 업무실적 | `8/12`(수), `8/14`(금) |
| 업무계획 | `8/19`(수) |

2026년 8월의 월요일: 3, **10**, 17, 24, 31
→ 실적 주간 = 8/10 주 = **2번째 월요일** → `8월 2주차` ✔ 파일명 일치

**규칙 확정**: `N주차 = 그 주 월요일이 그 달의 N번째 월요일`
스펙 §4.1의 예시(2026-08-17 → 8월 3주차)와도 일치.

부수 확인: 문서 구조가 **금주 실적 + 차주 계획**이다. 화요일 14:00 마감과 모순 없다.

---

### ✅ Cloudflare Tunnel 설정 파일 위치 → **파일이 없다 (원격 관리형)**

```
$ systemctl status cloudflared
  Active: active (running) since 2026-06-17  (1개월 26일)
  /usr/bin/cloudflared --no-autoupdate tunnel run --token eyJhIjoi…

$ ls /etc/cloudflared/config.yml   → 없음
$ ls ~/.cloudflared/config.yml     → 없음
```

토큰 방식이므로 **Cloudflare 대시보드가 설정의 정본**이다.

| 항목 | 값 |
|---|---|
| Account ID | `<CF_ACCOUNT_ID>` |
| Tunnel ID | `<CF_TUNNEL_ID>` |

→ **서버에서 편집할 파일이 없고, cloudflared 재시작도 불필요하다.**
   기존 서비스에 영향 없이 대시보드 작업만으로 추가된다. 절차는 [AU-11](docs/spec/03-auth.md).

---

### ✅ 포트 → **11111** (사용 중이지 않음)

```
$ ss -tln | grep -E ':(10000|11111) '
(없음)
```

`10000`·`11111` 모두 비어 있다. 사용자 지정대로 **11111** 채택.
다만 이 서버는 `80`, `3100`, `3101`, `5000`, `8003`, `8005`, `9400`, `10345` 등을
`0.0.0.0`에 열어 두고 있으므로, **반드시 `127.0.0.1`에 바인딩**한다 (OPS-03).

---

## 3. 요약

| | 항목 |
|---|---|
| 🔴 **차단** | Q-02 (8명 명단) — Phase 1 착수의 유일한 블로커 |
| 🔴 **Phase 2 차단** | Q-01 (한글 열기 확인) — Phase 1에는 영향 없음 |
| 🟡 진행 중 확인 | Q-03 ~ Q-10 |
| ✅ 해소됨 | 원 스펙 §10의 4개 중 **3개** + 포트 |

**Q-02만 답해 주면 Phase 1 구현을 시작할 수 있다.**
(팀 배정 없이 이름·이메일만으로도 시작 가능)
