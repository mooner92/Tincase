# 미결 사항

**🔴 착수 전 답 필요 · 🟡 진행 중 확인 · ✅ 해소됨(결정·근거 포함)**

v2 (2026-08-13): 멀티부서 확장으로 전면 갱신. 인물 명단은 `docs/private/seed.json`(git 제외) 참조.

---

## 1. 열려 있는 것

### 🔴 Q-01 — 생성한 hwp가 한글에서 정상적으로 열리는가 *(Phase 2 게이트)*

`fixtures/verify-write-test.hwp`를 Windows 한글에서 열어 확인.
`1-1` 행에 `AI데이터팀 주간회의 및 시스템 점검 (자동병합 테스트)` + 표 서식 무결이면 통과.

**한 번의 확인으로 두 가지가 판정된다**: ① 레코드 수술의 정합성 ② `cfb`가 주입하는
`\x01Sh33tJ5` 센티널 스트림(제거 불가, [R-001 §7.1](docs/research/001-hwp-format-findings.md))을 한글이 무시하는지.
실패 시 [ADR-0001](docs/adr/0001-hwp5-binary-record-engine.md) 재검토 (최소 CFB 라이터 자체 구현 → `.hwpx` 전환 순).
Phase 1에는 영향 없음.

### 🔴 Q-11 — 테넌트 경계: 부서 = 보고 단위인가? ★ 신규

실측([R-002 §4](docs/research/002-kei-org-and-collection-flow.md))에서 두 사례가 관찰됐다:

1. 기획조정실은 실무자 4명이 있고, 게시판에 **별도 보고**(`예산_대정부 주간업무`)가 따로 올라간다
2. 하위 실 소속 실무자가 **상위 본부 명의**로 제출한다 (탄소중립에너지연구실 → 기후대기전략연구본부 보고)

현재 스펙은 "1 부서 = 1 테넌트 = 1 병합본"이다. 질문:

- 한 부서가 **여러 종류의 주간보고**를 내는 경우(기획조정실)를 지원해야 하는가?
- 지원한다면 테넌트를 "부서"가 아니라 "보고 단위"로 잡아야 한다 (스키마 영향 큼)

➡️ **권고: Phase 1은 1부서=1보고로 고정**하고, 기획조정실 같은 복수 보고 부서는
온보딩 시점(Phase 3)에 "보고 단위" 분리를 재검토. 파일럿(AI홍보전략실)은 어차피 1보고.

### 🟡 Q-12 — AI홍보전략실 제출 대상(onRoster) 초기값

명부상 13명 (실장 1 + 담당 12). 원 스펙은 "8명"이었다.
초기 `onRoster=true` 대상을 확정해 달라 — 실장 포함 여부, 인턴 포함 여부.
(운영 중 담당자 화면에서 언제든 조정 가능 — DM-04. 시드 초기값만 필요)

### 🟡 Q-13 — Cloudflare Access 정책 방식 (확산 대비)

파일럿은 이메일 나열로 충분하다. 전 부서 확산 시 337명 나열은 유지 불가.

➡️ **권고: `Emails ending in @kei.re.kr` 도메인 규칙**으로 바꾸고,
실제 가입 게이트는 앱 DB(AU-04)가 담당. Access는 "KEI 구성원인가"만, 앱은 "등록 사용자인가"를.
파일럿 기간에 미리 전환해 두면 확산 시 정책 변경이 없다.

### 🟡 Q-14 — 운영자 가시성 범위

현재 스펙(AU-15): 운영자는 테넌시 관리만, **부서 내용·현황 접근 불가** (감시 금지의 보수적 해석).
운영 지원("업로드가 안 돼요" 문의) 시 불편할 수 있다. 이대로 갈지, 제한적 완화
(예: 부서별 "제출 건수" 숫자만)를 둘지.

➡️ **권고: Phase 1은 완전 차단으로 시작.** 문의 대응은 감사 로그(AuditLog)로 충분한지 파일럿에서 확인.

### 🟡 Q-15 — 부서 내부 가시성

현재 스펙(AU-06): member는 **자기 제출물만** 본다. 같은 부서원끼리도 서로 안 보인다.
"남의 부서" 감시 금지를 부서 안까지 보수적으로 확장한 기본값인데, 부서 내부까지
가릴 필요가 없다면 (예: 부서원도 부서 현황을 보게) 완화 가능.

➡️ **권고: 기본값(자기 것만) 유지.** 현황은 담당자의 것. 파일럿 피드백으로 재검토.

### 🟡 Q-16 — 병합 규칙 문법 확정 *(Phase 2 착수 시)*

[HM-18](docs/spec/08-hwp-merge-engine.md)의 제안 문법(`순서:` `빈행유지:` `특이사항:`)으로 충분한가?
추가로 필요한 지시어가 있으면 실제 부서 관례에서 수집한다. LLM 해석은 배제(결정성).

### 🟡 Q-17 — Docker data-root 이전 (프로젝트 범위 밖 기록)

`/var/lib/docker`가 98% 찬 루트 디스크에 있다. `/data`로 이전하면 근본 해결되지만
**이 서버의 다른 컨테이너 서비스 전부에 영향** — 별도 작업으로 판단할 것 (OPS-19는 완화책만).

### 🟡 Q-18 — 서브도메인 + 슬러그 스타일

- 서브도메인: `worklog.excusa.uk` 가정 유지?
- 페이지 슬러그: 사용자 예시대로 긴 영문명(`/AI_and_Public_Relations_Division`).
  짧은 별칭(`/aiprd`) 병행이 필요하면 알려달라 — 링크를 메신저로 공유할 때 길이가 거슬릴 수 있다.

### 🟡 Q-19 — 배포 시점 · 파일럿 시작 주차

첫 1~2주는 이메일 병행 권고 (OPS-18). 파일럿 시작 주차를 정해 달라.

---

## 2. 해소된 것 ✅

### ✅ 접근 방식 — 웹 업로드 방식 확정 *(사용자, 2026-08-13)*

담당자의 배포·취합 편의가 목적. CLI-first 대안은 기각. Phase 1 = 웹 플랫폼.

### ✅ 마감 예외 — **없음** *(사용자 확정, 2026-08-13)*

대리 업로드·지각 구제 일절 없음. 놓치면 다음 주차. "실무자만 수고가 늘어나는" 예외는 만들지 않는다.
→ [DM §4.1](docs/spec/01-domain-model.md), API-11. (구 Q-04·Q-06 종결 — 정각 14:00:00은 허용, `>` 비교)

### ✅ 파일 형식 — **hwp only** *(사용자 확정, 2026-08-13)*

`.hwpx` 접수 안 함. 양식을 웹에서 배포하므로 형식 통제 가능. 거부 시 변환 방법 안내.
→ ST-05, HM-23 종결. (구 Q-05)

### ✅ 리마인드 — 별도 기능 불요 *(사용자: "중복", 2026-08-13)*

담당자 화면의 `미제출 N명 복사` 버튼(PG-23)으로 충분. 자동 알림은 Phase 3 재검토 항목으로만 유지.
(구 Q-03·Q-10)

### ✅ 명단 — 인사자료 xlsx 확보 *(2026-08-13)*

337명·30개 부서·전원 이메일 보유. `tools/extract-seed.py` → `docs/private/seed.json`(git 제외).
AI홍보전략실 13명, 취합게시판 실무자 12명 lead 표시. (구 Q-02)
잔여: 초기 onRoster만 (Q-12).

### ✅ 저장 위치 — `/data` *(사용자 지시 + 실측 근거)*

`/` 98% (11G) vs `/data` 21TB 여유. 백업은 `/mnt/backup`(NFS 227T). → ST-00, OPS-08.

### ✅ 확장자·필드·N주차 규칙·터널 설정 *(R-001, 구 스펙 §10)*

`.hwp` 5.0 바이너리 확정 / 누름틀 없음 / `N주차 = N번째 월요일` 실데이터 검증 /
터널은 원격 관리형(서버 설정 파일 없음, 재시작 불필요). 포트 11111 비어 있음.

---

## 3. 요약

| | 항목 |
|---|---|
| 🔴 Phase 1 전 | **Q-11** (테넌트 경계 — 권고안 수락이면 즉시 해소), Q-12 (onRoster 초기값) |
| 🔴 Phase 2 전 | Q-01 (한글 열기 확인) |
| 🟡 진행 중 | Q-13~Q-19 |

**Q-11·Q-12만 답하면 Phase 1 구현 착수 가능.**
