# Changelog

## v1.8.1 — 2026-08-18

### 안내를 드롭다운에서 상단 메뉴로

안내는 **처음 쓰는 사람**이 찾는 것이다. 드롭다운 안은 이미 아는 사람만 여는 자리라
정작 필요한 사람에게 안 보인다. 상단 메뉴 맨 끝으로 옮기고 `?`와 외곽선을 붙여
업무 메뉴(제출·내 이력·수합 관리…)와 성격이 다르다는 것을 표시했다.

### 시연 화면에 창 테두리

시연 화면도 흰 바탕이고 안내 페이지도 흰 바탕이라 그냥 얹으니
**어디까지가 화면이고 어디부터가 페이지인지** 구별되지 않았다.

밝기로 계단을 만들었다 — 페이지(흰색) → **매트 `#e7e9ea`** → 창 제목바(`#f7f7f7`) →
화면(흰색). 여기에 테두리 한 줄과 그림자를 더했다.
처음에는 매트를 `#f7f7f7`로 뒀는데 흰색과 대비가 3%밖에 안 나 경계가 그대로 안 읽혔다.

제목바에는 창 점 세 개와 그 클립의 제목을 넣었다 — 스크롤 중간에 들어와도
"이건 화면 속 화면"이라고 바로 읽힌다.

## v1.8.0 — 2026-08-18

### 사용 안내 페이지 — 글 대신 움직이는 화면

`/guide`. 글로 "제출 버튼을 누르고…"라고 쓰면 읽는 사람이 자기 화면과 대조해야 한다.
움직이는 화면은 대조가 필요 없다.

네 가지를 담았다 — ① 양식 받아 올리기 ② 웹에서 작성(한글 표 붙여넣기)
③ 잘못 낸 것 취소 ④ 담당자의 수합·병합. 각 화면 아래에 **GIF 받기**를 뒀다
(한글 문서·메일에 붙여넣을 때). 웹에서는 같은 그림의 WebP를 쓴다 — 용량이 1/6이다.

### 화면은 가짜 부서에서 찍는다

이 저장소는 public이다. 실제 화면에는 부서원 실명이 그대로 나오므로, 실제 데이터로
녹화해 커밋하면 개인정보가 저장소에 박힌다. **안내 자료는 한 번 만들어 오래 쓰는 것**이라
나중에 지우기도 어렵다.

그래서 `scripts/seed-demo.ts`가 이름·부서·업무 내용이 전부 가공인 데모 DB를 만들고,
거기에 띄운 서버에서 녹화한다. 부수 효과로 **매번 같은 상태에서 시작**하므로
다시 찍어도 결과가 같다.

### 녹화는 `recordVideo`가 아니라 프레임을 직접 찍는다

Playwright의 `recordVideo`(WebM)를 먼저 검토했는데 세 가지가 걸렸다.

1. **커서가 안 나온다.** Playwright 영상에는 마우스 포인터가 렌더되지 않는다.
   포인터 없이 화면만 바뀌면 무슨 일이 일어난 건지 알 수 없다 — 안내 자료로는 치명적이다
2. **타이밍을 못 잡는다.** "여기서 1.2초 머문다"가 안내에서는 중요한데
   영상 → GIF 변환에는 그 개념이 없다
3. 이 서버에 ffmpeg가 없다

그래서 커서를 DOM으로 그려 넣고(클릭 파문·자막 포함), 프레임을 직접 찍어
지속시간을 프레임별로 준다. 조립은 Pillow가 한다 — ffmpeg 없이 GIF·WebP 둘 다 나온다.

용량은 세 가지로 잡았다: 폭 900px · **연속된 같은 프레임 병합**(정지 구간이 대부분이다) ·
전체 프레임에서 뽑은 **단일 팔레트**(프레임마다 뽑으면 같은 회색이 프레임마다 달라져 깜빡인다).
디더링은 껐다 — UI는 평면 색이라 도움이 안 되고 점 패턴이 프레임 간 차분 압축을 망친다.

    ① 제출     5.7초  GIF  765KB · WebP 106KB
    ② 웹작성   7.2초  GIF 1493KB · WebP 206KB
    ③ 취소     4.1초  GIF  512KB · WebP  81KB
    ④ 병합     8.5초  GIF  968KB · WebP 187KB

## v1.7.2 — 2026-08-17

### 헤더 로고의 밑동이 잘려 있었다

사용자 지적. 재보니 lockup 5개 파일 전부 **내용이 viewBox 아래로 6단위 삐져나가** 있었다
(높이 84 기준 7%). 틴케이스 심볼의 둥근 밑동이 직선으로 잘려 보이던 이유다.

원인은 **획(stroke)이 패스 바깥으로 절반 튀어나온다는 것**을 계산에 넣지 않은 것이다.
심볼 바닥 패스는 `y=88`에서 끝나지만 `stroke-width:8`이라 실제로 칠해지는 곳은 `y=92`다.
viewBox는 `y=86`에서 끝났다.

이전 커밋에서 "12개 파일의 잘림을 고쳤다"고 했는데 그때 쓴 `getBBox()`가
**기본적으로 획을 빼고 잰다.** 잘못된 자로 재고 고쳤다고 한 것이다.
이번에는 큰 캔버스에 그려서 **칠해진 픽셀의 경계**를 쟀다 — 획·linecap·모서리가 전부 들어간다.

- lockup 5종 `viewBox` → `4 2 361 96` (사방 여백 6단위, 워드마크·스택형과 동일 규칙)
- 왼쪽 여백도 18 → 6으로 통일 (혼자 3배였다)
- viewBox 높이가 84→96이라 같은 `h`면 12% 작아지므로 헤더를 `h-26px` → `h-30px`로 보정.
  **로고 크기는 그대로 두고 잘린 부분만 되살렸다**
- 자산 21개 전부 재검증 — 잘림 0

`public/brand/README.md`에 검증 방법을 적어 뒀다. `getBBox()`로 확인하면 또 속는다.

## v1.7.1 — 2026-08-17

### `reset-week`가 "파일 0개"라고 거짓 보고했다

운영 서버에서 모의 테스트를 정리했더니 **DB 행은 지워졌는데 파일 5개가 그대로 남았다.**
그런데 스크립트는 "파일 0개"라고만 찍었다 — 0개를 지웠다는 뜻인지, 지울 게 없었다는
뜻인지 구별되지 않는 문구다.

원인은 `catch { return false }`였다. 컨테이너가 uid 10001로 파일을 만들고
디렉터리에 group write가 없어 호스트 계정의 `unlink`가 EACCES로 실패했는데,
그 오류를 catch가 삼켰다.

**정리했다고 보고하고 정리가 안 되는 것**이 이 스크립트의 최악의 실패다.
발표 전날 "깨끗합니다"를 믿고 갔다가 남의 이름이 든 파일이 나오면 그게 사고다.

- `ENOENT`(이미 없음)와 진짜 실패를 구분한다
- 실패는 파일별로 출력하고, 마지막에 고아 파일 경고 + **종료 코드 1**
- 운영 서버에서는 컨테이너 안이나 root로 실행해야 한다는 안내를 붙였다

### `scripts/restore-passwords.ts` — 배포본 비밀번호로 되돌리기

테스트하느라 남의 계정 비밀번호를 바꿔 놓으면, 그 사람은 나눠 받은 안내문으로
로그인할 수 없게 된다. `password-reset` API는 **새** 임시 비밀번호를 만들기 때문에
이 상황을 못 푼다 — 개인별로 다시 전달해야 한다.

배포 CSV의 값으로 되돌리면 아무 일도 없었던 게 된다. 되돌리면 변경 강제가 다시 켜지고,
실패 카운트·잠금이 풀리며, 살아 있는 세션이 전부 끊긴다 — "한 번도 안 들어간 상태".

CSV 경로는 인자로 받는다. 비밀은 코드에 없다.

## v1.7.0 — 2026-08-17

### 제출 취소 — 본인은 마감 전까지, 운영자는 언제든. **담당자는 못 한다**

지금까지 제출물은 지울 수 없었다. 잘못 올렸으면 올바른 파일로 덮어 v2를 만들 뿐,
"안 낸 상태"로는 못 돌아갔다. 모의 테스트 데이터도 화면에서 치울 수 없었다.

| 주체 | 삭제 | 조건 |
|---|:---:|---|
| 본인 | ✓ | **마감 전까지만** |
| 관리자(operator) | ✓ | 부서·마감 무관 + 감사 로그 |
| 부서담당자(lead) | ✗ | 읽을 수는 있어도 지울 수는 없다 |
| 총괄담당(coordinator) | ✗ | 위와 같다 |

**담당자를 뺀 것이 이 변경의 핵심이다.** 담당자가 부서원의 제출을 지울 수 있으면
현황표의 `○ 미제출`이 "안 냈다 **또는** 담당자가 지웠다"가 된다. 두 상태가 한 칸에 겹치면
목요일 2시에 그 표를 보고 독촉할지 말지 판단할 수 없다. 담당자에게 필요한 건
삭제가 아니라 재제출 요청이고, 독촉 버튼은 이미 있다.

**마감이 본인을 막는 이유**는 병합본에는 들어갔는데 원본은 없는 상태를 막기 위해서다.
마감은 제출뿐 아니라 **철회에도 걸린다.**

**삭제는 그 주차 전체다.** v2만 지우고 v1을 남기면 현황은 `● 제출`인데 최신 내용은
사라진 상태가 된다 — 이력이 거짓말을 한다.

**되돌릴 수 없다.** 파일까지 지운다. 대신 감사 로그가 파일명·크기·sha256·버전 목록을
**파일이 사라지기 전에** 붙잡아 둔다. 복구는 못 해도 무엇이 있었는지는 남는다.

접근제어 헌법을 먼저 고쳤다 — [TACP](TACP.md) v1.1에서 `delete`를 `read`·`write`와
별개 Action으로 신설하고, TACP-8("readAll은 읽기만이다")에 예외 하나를 명시했다.
예외는 `readAll`이 아니라 **`isOperator`에 붙는다** — 총괄담당은 readAll을 공유하지만
삭제하지 못한다. 근거는 [ADR-0007](docs/adr/0007-submission-deletion.md).

검증: 담당자 화면에서 열기 10건은 뜨고 삭제 버튼은 0개, **API를 직접 호출해도 404**
(버튼만 숨긴 게 아니다). 테스트 163개(신규 6개 — 새로 허용된 것과 새로 금지된 것 둘 다).

## v1.6.1 — 2026-08-17

### "휑함"의 원인은 여백이 아니라 폭이었다

깔끔하게 만들었더니 비어 보인다는 지적. 장식을 더하는 대신 원인을 재봤다.

- **본문 폭 1280 → 1120px.** 제출 화면의 내용은 그대로인데 1280px에 펼치면
  드롭존이 710×170의 납작한 띠가 되고, 표는 컬럼이 벌어진다.
  요소를 키운 게 아니라 **판을 줄여** 같은 내용이 밀도 있게 읽히도록 했다.
- **푸터 한 줄 추가.** 내용이 짧은 화면은 아래로 그냥 흰 화면이 이어져
  페이지가 끝난 건지 덜 그려진 건지 알 수 없었다. 경계선 하나로 바닥을 만든다.
  내용은 장식이 아니라 실용 — 막혔을 때 제일 먼저 찾는 **문의처**를 넣었다.
- 내용이 짧아도 푸터가 화면 바닥에 붙도록 (`flex-col` + `flex-1`).
  여백이 "페이지 안"에 갇히지, "페이지가 모자란" 것으로 보이지 않는다.

## v1.6.0 — 2026-08-17

### 디자인 전면 개편 — Airbnb 구조 + Tincase 그린

크림 캔버스와 채도 높은 포인트 카드(teal/peach/lavender/ochre)를 걷어내고,
**흰 캔버스 · 넉넉한 여백 · 부드러운 모서리 · 절제된 굵기 · 강조색 하나**로 바꿨다.

색을 여러 개 쓰면 강조가 강조를 잃는다. Airbnb가 Rausch 하나로 모든 CTA를 끌고 가듯,
우리는 **로고의 딥 그린 `#0a3711` 하나**만 강조색으로 쓴다. 나머지는 흑백 계조다.

| | 이전 | 이후 |
|---|---|---|
| 캔버스 | 크림 `#fffaf0` | 흰색 `#ffffff` |
| 강조 | teal·peach·lavender·ochre·amber 5색 | 그린 1색 |
| 버튼 | 12px 모서리 · 44px | 8px · 48px |
| 카드 | 16~24px | 14px (피처 20px) |
| 그림자 | 없음 | **한 단계만** (떠오른 카드·드롭다운) |

- 로고를 새 녹색 세트로 교체. 12개 파일이 오른쪽·아래로 잘리고 있어 실제 경계를 계산해 고쳤다
- 조직도 SVG의 하드코딩된 주황색을 녹색 계열로 (초록 배경 위에서 충돌했다)
- 표 안 '열기' 버튼 12개가 전부 진초록이면 강조가 아니다 → 외곽선 버튼으로
- '빈 양식 받기'가 초록 카드면 '제출 완료' 초록과 경쟁한다 → 흰 카드 + 초록 CTA만

### 드로어 — 아래 밝은 줄 제거

패널이 뷰포트보다 24px 짧아 아래로 페이지가 비쳤다. 높이를 명시하고
그림자 대신 **세로 경계선 하나**만 남겼다 (드로어 3종 동일하게).

기능 회귀 17항목 확인: 드로어·버전전환·붙여넣기·비우기·병합·설정·운영·조직도·
감사로그·감사문서·CSV·병합본 받기 전부 정상, JS 오류 0.

## v1.5.2 — 2026-08-17

### 🔴 한글 표 붙여넣기가 칸을 행으로 만들던 문제

**한글은 클립보드 평문에 셀을 줄바꿈으로 넣는다** (엑셀처럼 탭이 아니다).
4열 4행을 복사하면 16줄이 되고, 칸 하나가 행 하나가 됐다. 엑셀 기준으로만 만든 탓이다.

클립보드에는 `text/html`이 함께 들어오고 거기엔 진짜 `<table>`이 있다 →
**HTML을 먼저 보고, 없을 때만 평문을 본다.** 덤으로 셀 안 줄바꿈이 한 줄로 합쳐진다
("제5차 국가환경종합계획 / 수정계획 공청회" → 한 줄).

### 한 번에 지우기

잘못 붙여넣으면 12줄을 하나씩 지워야 했다. 구역별 **비우기**와 **전체 지우기**를 뒀다.

### 가독성

줄 높이를 낮춰 세 구역이 한 화면에 들어온다 (12줄 붙여도 스크롤 없이 보인다).
칸 너비를 한글 표 비율에 더 맞추고, 줄 교차 배경을 진하게 했다.

## v1.5.1 — 2026-08-17

### 한글 표를 그대로 붙여넣기

한 칸씩 옮겨 적으면 웹 작성을 쓸 이유가 없다. 이미 한글에 표가 있다면 통째로 긁어
붙이는 게 자연스럽다. 한글·엑셀에서 표를 복사하면 클립보드에 **탭으로 나뉜 글자**가
들어오므로 그걸 해석한다.

- 머리글 행(`구분 / 업무실적 내용 / …`)이 딸려 와도 버린다
- `구분` 열(`1-1`)도 버린다 — 시스템이 다시 매기므로(ABS-5) 받을 이유가 없다
- 4열·3열·내용만 여러 줄 등 어떻게 잘라 복사해도 받는다
- **평범한 글자는 표로 보지 않는다** — 한 칸에 긴 글을 붙이는 것도 정상적인 사용이다

### 작성 화면 가독성

한글 표와 **같은 모양**으로 바꿨다. 부서원은 한글에서 그 표를 채워 왔으므로 다른
배치를 내밀면 어디에 뭘 넣는지 다시 배워야 한다.

- 머리글(구분·업무 내용·일자·장소·참석자)을 실제 표처럼 고정
- 줄마다 번갈아 배경, 칸 높이·너비를 한글 표 비율에 맞춤
- `구분`은 입력칸이 아니라 번호 표시 · 붙여넣기 안내를 상단에 상시 노출
- 바닥에 실적/계획/특이 줄 수를 각각 표시

### 문서 이름 규칙 하나로

다섯 군데가 제각각이었다 — 연도가 있기도 없기도, 부서가 빠지기도, 순서도 달랐다.
파일이 쌓이면 **이름이 곧 분류**라서 규칙이 흔들리면 정렬도 검색도 안 된다.

```
{연도}_{M월 W주차}_{부서}_{무엇}
2026_8월_3주차_AI홍보전략실_주간업무.hwp
```

연도를 앞에 둬 이름순 정렬이 시간순이 되게 했다.

### 부수

- 문서 맨 끝에 본문 문단을 덧붙이는 기능 (병합일자용). 표는 건드리지 않고 기존 문단을
  복제한다. **맨 끝에만** 붙인다 — 첫 문단에는 구역 정의가 들어 있어 앞·사이는 위험하다
- 마감 잠금 테스트가 **월요일에만** 깨지던 문제. '어제 요일'로 계산하면 월요일엔
  어제=일요일이 되고 그건 이번 주차의 마지막 날이라 미래가 된다. 주 경계에서 뒤집히는
  요일 산술을 없앴다 (일요일에도 같은 일을 겪었다)

## v1.5.0 — 2026-08-16

### 웹에서 작성해 바로 제출 (S-10)

한글을 열지 않고 화면에서 적어 제출한다. **파일 업로드는 그대로 둔다** (WA-02) —
한글에서 표·서식을 쓰는 사람이 있고, 웹 작성이 막혔을 때의 폴백도 필요하다.
부서원은 매주 둘 중 하나를 고른다.

- 표를 그대로 흉내 내지 않는다. 채우는 건 결국 **한 줄에 네 칸**이고 `구분`은
  시스템이 다시 매기므로(ABS-5) 사람이 볼 이유가 없다
- 마지막 줄을 채우면 새 줄이 따라온다 — "추가" 버튼을 누르러 가지 않게
- 제출하면 **부서 양식으로 hwp를 만들어** 기존 업로드와 같은 경로로 저장한다.
  검증·버전·감사 로그가 전부 동일하고, 담당자 화면·zip·병합이 그대로 동작한다
- 생성물도 자체 검증을 통과해야 저장된다 (WA-05) — 우리가 만든 것이라고 봐주지 않는다
- `Submission.origin`으로 만든 경로만 구분해 둔다 (통계·디버깅용)

### 모의 테스트 준비물

- `make-test-files.ts` — 사람마다 다른 내용의 hwp를 부서 양식으로 뽑는다.
  일부러 겹치는 업무를 섞어 중복 묶기를 확인할 수 있게 했다. DB는 건드리지 않는다
- `reset-week.ts` — 한 주차의 제출물·병합 기록을 지운다. **기본은 미리보기**이고
  `--yes` 가 있어야 실제로 지운다. 감사 로그는 남긴다 — 테스트를 언제 했는지도 기록이다

## v1.4.1 — 2026-08-16

### 🔴 한글이 병합본을 못 열던 문제 (Q-01 종결)

생성한 hwp를 한글이 "파일이 손상되었습니다"로 거부했다. **빈 칸에 문단끝 표시만 든
빈 글자 레코드를 만들어 넣은 것**이 원인이었다. 한글 양식은 빈 칸에 그 레코드를 두지 않는다
(원본 실측: 빈 셀 20개 전부).

원인을 좁힌 과정 — 각 동작을 분리해 한글에서 하나씩 확인했다:

```
껍데기만 재작성        → 열림    글자만 교체          → 열림
빈 칸에 글자 넣기      → 열림    행 줄이기            → 열림
행만 20개로 늘림       → 열림    행 늘림 + 글자 채움  → 손상 ★
```

마지막 조합만 깨졌고, 그 경로에서만 **빈 글자**를 쓰고 있었다.
셀 구성을 세어보니 그대로 드러났다: 정상 파일은 글자 없는 셀이 68개, 깨진 파일은 0개.

**검증 원칙을 바꿨다.** LibreOffice 개봉은 통과 근거가 못 된다 — 깨진 파일도 열어준다.
대신 원본 양식과 레코드 구성을 대조하는 검사를 게이트로 뒀다 (HM-T30~34).

### 부수

- cfb 라이브러리가 쓰기마다 주입하는 표식 스트림 제거 (HM-08a). 원인은 아니었지만
  원본과 스트림 목록이 달라질 이유가 없다
- 로고 세로형이 아래·오른쪽에서 잘리던 문제 — viewBox가 내용보다 작았다 (파일 자체 버그)
- 조직도에 **실제로 업무일지를 내는 부서만** 그린다. 30개를 다 그리면 227명이 회색 점으로
  원 둘레를 채워 정작 볼 것이 안 보인다

테스트 147개.

## v1.4.0 — 2026-08-16

Phase 2 착수. **병합이 실제로 돌아간다.**

### 자동 병합 (HM-16/19/24/25)

목표 시나리오는 하나다 — **목요일 14:10에 들어오면 이미 되어 있다.**
그래서 병합은 버튼이 아니라 마감이 시킨다 (5분 주기 스케줄러, 부서별 1회).

- **hwp 쓰기 계층** — 표 행 확장·축소. 새 레코드를 지어내지 않고 **같은 표의 마지막
  데이터 행을 복제**한 뒤 좌표와 글자만 바꾼다. 서식이 깨질 여지가 구조적으로 없다
- **모델은 판단만, 조립은 코드가** — 모델은 한 글자도 출력하지 않는다. id 묶음만 낸다.
  문서 글자는 전부 작성자 원문 그대로
- 결정론 중복 탐지(정규화 완전일치)를 바닥에 깔고 모델을 얹는다. 모델이 죽어도 병합은 완결
- **글자 겹침 하한 0.5** — 모델이 "언론"이라는 공통 주제만 보고 다른 업무를 합치려 한
  실측 사례를 막는다. 주제가 같은 것과 같은 업무인 것은 다르다
- 응답 스키마를 ollama에 직접 준다. `format:'json'`은 모양을 강제하지 않는다 (실측: 9B가
  입력을 되돌려준 적 있음). 스키마를 주니 그 실패 모드가 사라지고 8.6초 → 3.6초

실측 (AI홍보전략실 12명, 실적 52행): 41초에 실적 22 · 계획 15 · 특이 1, 중복 26건 묶임.

### 병합 규칙 — 문법을 없앴다 (HM-18 개정)

초안의 `순서:` `빈행유지:` 지시어 문법을 폐기했다. 실제 부서가 보낸 규칙은 두 줄이었고
**권장이지 의무가 아니었다.** 30개 부서 중 문법을 쓴 곳은 0곳.
남은 것은 설정 3개(분류 순서·중복묶기·특이사항) + 자연어 지침 1개.

**분류 정렬 (HM-27)** — "순서: AI-홍보-시스템-도서관"은 사람 순서가 아니라 **업무 분류
순서**다. 그래서 병합본이 사람별 블록이 아니라 주제별로 묶인다.

### 검토 화면 (HM-26)

"슥 보고 제출"이 되려면 전체를 다시 읽게 하면 안 된다. **기계가 판단한 곳만** 보여준다 —
합쳐진 행(원문 나란히 + 어느 것을 남겼는지), 경고, 미제출자, 모델 사용 여부.

### 전사 조직도 모니터 · 감사 문서

- `/ops/monitor` — 연구원 → 본부 7 → 실 30 → 사람 337 방사형 트리.
  제출한 사람 → 담당자 간선만 초록으로 흐른다
- **집계 대상을 실제 제출하는 부서로 좁혔다** — 취합게시판 이력 실측(R-002) 기준
  11개 부서. 연구부서 19개(227명)는 애초에 업무일지를 내지 않아 분모에서 뺀다.
  안 그러면 12/307(4%) 같은 무의미한 숫자가 나온다
- `/api/ops/report` — 조직도 + 부서별/개인별 표. HTML(인쇄→PDF) · CSV(엑셀, BOM)

### 접근제어

- **TACP.md 제정** — 접근제어 헌법. Principal 4종·권한 행렬·불변식 13개·게이트·변경 절차
- 병합본은 제출물과 다른 자원 (TACP §3.2) — 부서가 대외로 내보내는 산출물이라
  공개 범위가 한 단계 넓다. 대신 member에게는 여전히 닫혀 있다
- operator 판정이 4개 라우트에 복사돼 있던 것을 게이트로 통합. 재발 방지 테스트 추가

### 브랜드

제품명 **Tincase**. 로고·파비콘 적용.

테스트 134개.

## v1.3.1 — 2026-08-14

### 🐞 수정: 타 부서 페이지에서 본문이 내 부서 데이터를 보여주던 문제

`/kaccc` 로 들어가면 헤더는 **국가기후위기적응센터**인데 부서 제출 현황은
**AI홍보전략실**이 나왔다. 레이아웃만 URL 슬러그를 해석하고 페이지는 `scope.division`
(내 부서)을 쓰고 있었다.

권한 구멍은 아니었지만(운영자가 볼 수 있는 데이터였다) **오독을 유발하는 치명적 버그**다 —
남의 부서 현황을 자기 부서로 착각하면 판단을 그르친다.

- **`resolveTargetDivision()` 단일 출처 도입** (AU-17). 페이지·API가 모두 이걸 통과한다
- `getDivisionView(slug)`가 `{division, isOwn, canManage, canSubmit}`를 주고,
  레이아웃과 페이지가 **같은 부서**를 본다
- 제출·수합 관리·부서 설정 전 페이지를 해석된 부서 기준으로 교체

### 타 부서 열람 시 안전장치 (AU-17c/d)

- 헤더 배지에 **`열람`** 표시 + 상단 배너로 "내 부서가 아님 · 열람은 기록됨" 명시
- 타 부서에서는 **제출·양식 내려받기·설정 편집 불가** (읽기 전용).
  업로드는 신원 기준으로 내 부서에 저장되므로(DM-12) 그런 버튼은 함정이라 아예 렌더하지 않는다
- 메뉴도 상황에 맞게: 타 부서에서는 `제출`→`개요`, `내 이력` 숨김
- zip API에 `?division=` 추가 — **readAll만** 타 부서 허용, 없으면 내 부서(안전한 기본값)

### 권한 확인

member·lead는 타 부서 슬러그·**별칭 모두 404** (기존 동작 유지, 회귀 테스트로 고정).
operator·coordinator만 열람 가능하며 감사 로그가 남는다.

테스트 102개 (신규 6 — 부서 해석 단일 출처 회귀).

## v1.3.0 — 2026-08-14

### 디자인 시스템 도입 ([docs/design-system.md](docs/design-system.md))

Clay 분석 문서를 사내 도구에 맞게 이식했다. 슬레이트 그레이 기본 스타일을
크림 캔버스 + 잉크 CTA + 헤어라인 카드 체계로 전면 교체.

- **Tailwind v4 `@theme` 토큰 단일 출처** — 컴포넌트 hex 하드코딩 제거
- 크림 캔버스(#fffaf0) · 잉크 CTA(#0a0a0a) · 1px 헤어라인으로 깊이 표현 (그림자 없음)
- 포인트 색에 **의미 부여**: teal=요약/집계, peach=행동 유도, mint=완료, coral=임박
- 라운드 상향: 버튼·입력 12px · 카드 16px · 피처 카드 24px
- `.btn-primary` `.card` `.badge-pill` `.input` `.tab-pill` 유틸 클래스로 정리
  (shadcn/ui 도입 계획은 폐기 — 의존성 없이 토큰과 1:1)

### 메뉴·페이지 구성 개편

- **`<AppHeader>` 단일 상단 내비** — 부서 페이지와 `/ops`가 공유. sticky 64px
- 메뉴가 역할에 따라 확장: `제출` `내 이력` → (lead) `수합 관리` `부서 설정` → (operator) `운영`
- **활성 메뉴 pill 표시** (`aria-current`) — 지금 어디인지 항상 보인다
- **비밀번호·로그아웃을 사용자 드롭다운으로** 이동 — 상단엔 행동 메뉴만
- 최대 폭 1280px 공통. 제출 화면은 **7/5 그리드** (좌: 제출, 우: 양식·부서 현황)
- 수합 관리 요약을 **teal 피처 카드**로 — 몇 명 냈는지가 한눈에

### 화면별 정리

- 제출: 주차가 페이지 제목(히어로). 제출 완료는 mint 카드, 양식 받기는 peach 카드
- 로그인·비밀번호: 카드 레이아웃 + display 헤딩
- 운영: 헤더 통합, 탭·표를 토큰 체계로

## v1.2.0 — 2026-08-14

### 기본 마감 목요일 14:00으로 변경

전사 취합게시판 마감이 **목 15:00**이므로, 부서 내부 마감을 목 14:00으로 두면
담당자가 병합·제출할 1시간이 남는다. 스키마 기본값·전 부서 30개 모두 적용.
부서별 조정은 `/ops`에서 가능.

### 취합게시판 제출 이력으로 부서 탭 분리 (DM-15)

30개 부서가 모두 제출하지는 않는다 — 상위 조직이 대표로 내거나, 하위 실 담당자가
상위 본부 명의로 내는 구조가 섞여 있다. 실제 제출 이력을 기록해 온보딩 우선순위로 삼는다.

| 탭 | 부서 | 근거 |
|---|---|---|
| 제출 확인 | **11** | 게시판에 제출일이 확인됨 |
| 확인 필요 | **2** | 기획경영본부(제출일 미표시) · 기후대기전략연구본부(하위 실이 본부 명의로 제출) |
| 이력 없음 | **17** | 게시판 미등장 |

출처는 2026-08-13 화면 관찰이라 확정 사실이 아니다. 상수 하드코딩이 아니라 DB 필드로 두어
운영자가 `/ops`에서 갱신할 수 있게 했다. 반영: `scripts/apply-board-history.ts`

### 인원 관리를 드로어로 (PG-33d)

부서가 30개라 표 아래에 펼치면 스크롤해야 보였다. 오버레이 드로어로 바꿔
**스크롤 없이 즉시** 보이게 했다. Esc·배경 클릭으로 닫기, 배경 스크롤 잠금,
헤더 고정 + 부서별 발급 현황 요약.

## v1.1.2 — 2026-08-14

### 문서 최신화

- **[docs/STATUS.md](docs/STATUS.md) 신설** — 구현/미구현을 코드 대조로 정리한 단일 출처.
  기능별 상태, 미구현 항목의 차단 요인, 가동은 하나 미완인 것, 수용된 위험까지
- README·ROADMAP을 현재 상태(v1.1.x, 사내망 가동)로 동기화

### 스펙-코드 불일치 수정

- `POST /api/division/merge` 라우트 신설 — 스펙(API-30)은 Phase 1에서 501을 반환한다고
  했으나 라우트가 없어 404가 나고 있었다. 계약을 코드로 고정하고 테스트 추가
  (member 404 · lead 501). Phase 2에서 클라이언트를 고칠 일이 없도록

### 발급 도구 개선

- `issue-passwords.ts`: `--bom`(Excel 한글 깨짐 방지), `--messages`(개인별 안내문) 추가
- 출력을 파이프로 자르면 SIGPIPE로 발급이 중단되어 비밀번호가 유실된다는 함정을 문서화

테스트 96개.

## v1.1.1 — 2026-08-14

### 운영자 비밀번호 초기화 (AU-27)

`/ops` 인원 목록에서 **누구든 비밀번호를 초기화**할 수 있다. 셀프 리셋(메일)이 불가능하므로
분실·잠금의 유일한 복구 수단이다.

- 새 임시 비밀번호를 **응답에 한 번만** 반환 (서버는 해시만 보관 — 놓치면 재초기화)
- 대상의 **전 세션 파기** + **잠금 해제** + 변경 강제 플래그
- 발급 결과 목록에 **복사**·**안내문 복사**(주소·아이디·임시비번) 버튼
- 인원 목록에 상태 배지: `미발급` / `변경 대기` / `사용 중` / `잠김`
- operator 외에는 404 (존재 은닉) · 감사 로그 `password_reset`

테스트 95개 (신규 4).

## v1.1.0 — 2026-08-14

**사내망 개통.** 이제 부서원은 `http://<서버-내부-IP>:11111` 에서 바로 쓴다.

### 사내망 비밀번호 인증 ([ADR-0006](docs/adr/0006-internal-password-auth.md))

배경: 사내 서버인데 사내 트래픽이 Cloudflare 엣지를 왕복하는 구조가 부적절했다.
실측 결과 KEI SMTP 릴레이(`spam.kei.re.kr`)와 LDAP/AD 모두 이 서버에서 닿지 않아,
자립적으로 가능한 방식은 비밀번호뿐이었다.

- **신원 제공자 2개, 인가 1개** — 사내망 세션 · 외부 Cloudflare JWT가 같은 seam 뒤에.
  인가·격리 코드는 한 줄도 바뀌지 않았다 (기존 인증 추상화 덕분)
- scrypt 해시(Node 내장, 네이티브 의존 없음) · 세션 **30일** 슬라이딩 · DB에는 토큰 해시만
- 운영자가 임시 비밀번호 발급 → **첫 로그인 시 변경 강제** (`scripts/issue-passwords.ts`)
- 연속 8회 실패 시 10분 잠금 · 사용자 열거 방지(동일 401 문구) · 변경 시 전 세션 무효화
- **Cloudflare Access는 운영자 외부 통로로 한정** (정책을 1명으로 축소)
- 바인딩을 `0.0.0.0:11111`로 개정 — 앱이 자체 인증하므로 LAN 노출이 정당해짐 (AU-01 개정)

### 전사 표준 양식 (ST-20)

기획조정실 공통 원본을 플랫폼에 두고, 각 부서 담당자가 받아 **자기 부서 부분만 남겨**
부서 양식으로 등록하는 실제 관례를 지원. `GET/POST /api/template/standard`.

### 그 외

- 부서 설정 화면에 "부서 양식 만드는 법" 안내 + 표준 양식 받기
- 로그인/비밀번호 변경 화면, 경로별 로그아웃(세션 vs Cloudflare)
- 웹 작성 기능 설계 문서 신설 ([S-10](docs/spec/10-web-authoring.md)) — Phase 2 이후 착수

### 테스트

91개 (v1.0.0 77 + 사내망 인증 14). 격리 스위트는 그대로 통과 — 인증 경로가 늘어도 격리는 불변.

### 알려진 제한

- **사내망은 평문 HTTP** — 비밀번호·세션이 LAN을 평문으로 지난다 (AU-26, 의식적으로 수용한 위험).
  확산 시 TLS 재검토
- 비밀번호 셀프 리셋 없음 (메일 발송 불가) — 운영자 재발급
- 자동 병합·웹 작성은 Phase 2 (Q-01 확인 대기)

## v1.0.0 — 2026-08-14

첫 정식 릴리스. KEI 부서 단위 주간 업무일지 수합 플랫폼 (파일럿: AI홍보전략실).

### 기능

- **부서별 페이지** `/{영문슬러그}` + 별칭(`/aiprd`) — 로그인하면 자기 부서로 자동 이동
- **업로드**: hwp 전용 검증(매직 넘버 + 구조 + 표 파싱), 버전 관리(재업로드 시 v2·이력 보관),
  드래그앤드롭 + 진행률, 마감 자동 잠금(부서별 정책, 기본 화 14:00)
- **부서 현황**: 부서원은 이름·제출여부·시각까지, 담당자는 전체(버전·크기·다운로드)
- **담당자 수합**: 현황표 · **열람 드로어**(파싱된 표 렌더·버전 전환·←→ 제출자 이동) ·
  개별/전체 zip 다운로드 · 미제출자 이름 복사 · 과거 주차 조회
- **부서 설정**: 양식 등록/교체(파싱 검증 + 이력 보관) · 작성 안내 · 병합 규칙 저장(Phase 2 대비)
- **운영(/ops)**: 부서 활성화·마감 정책·별칭 · 인원 배치(역할·제출 대상·정렬·계정)
- **양식 다운로드**: 파일명에 주차·부서명 자동 주입

### 보안·격리

- Cloudflare Access JWT 서명 검증 (`jose` — 헤더 신뢰 안 함), `127.0.0.1:11111` 바인딩
- 부서 간 완전 격리: member·lead에게 타 부서는 현황·존재까지 404 (구별 불가)
- 운영자·총괄의 타 부서 열람은 전부 감사 로그
- 업로더·부서는 항상 서버가 신원에서 도출 — 요청 본문의 지정 필드 무시 (사칭 불가)

### 엔진

- 자체 HWP 5.0 레코드 파서 (Node, 한글 프로그램 불필요) —
  왕복 바이트 동일성 게이트, 실제 KEI 양식 3종으로 검증
- 주차 계산: `N주차 = 그 달의 N번째 월요일`, 부서별 마감, 다중 타임존 안전

### 운영

- Docker standalone (비루트), 헬스체크(DB·저장소·양식·루트 디스크 감시)
- 데이터 `/data/worklog`, 백업 sqlite `.backup` → NFS(daily) + 파일(weekly), 복구 verify
- 시드: 인사자료 → 부서 30·사용자 337 (개인정보는 git 제외 경로에만)

### 테스트

77개 — 주차 27(×4 TZ) · hwp 파서 17 · 통합 33 (격리 릴리스 게이트 포함)

### 알려진 제한

- 자동 병합은 Phase 2 (버튼 자리만, 501)
- 총괄(coordinator) 대시보드 `/overview`는 확산 단계(Phase 3)
- Cloudflare Access 앱 연결 전까지는 `worklog.excusa.uk` 미개통 (docs/DEPLOY.md §3)
