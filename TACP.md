# TACP — Tincase Access Control Principal

> **이 문서는 Tincase 접근제어의 헌법이다.**
> 코드·스펙·UI가 이 문서와 어긋나면 **문서가 아니라 코드가 틀린 것**이다.
> 규칙을 바꾸려면 §10 절차를 따른다. 코드를 먼저 고치는 것은 위반이다.

버전 1.1 · 2026-08-17 · 대상 코드 v1.7.0
관련 스펙: [03-auth](docs/spec/03-auth.md) · [01-domain-model](docs/spec/01-domain-model.md) · [ADR-0005](docs/adr/0005-multi-division-tenancy.md) · [ADR-0007](docs/adr/0007-submission-deletion.md)

변경 이력: v1.1 — `delete` Action 신설, TACP-8에 예외 하나(TACP-14) 추가

---

## 왜 헌법이 필요한가

접근제어는 **한 곳만 틀려도 전부 무너지는 종류의 코드**다. 그리고 실제로 틀렸다.

v1.3.0에서 레이아웃만 URL을 해석하고 페이지는 신원의 부서를 써서, 헤더는 A부서인데
본문은 B부서가 나왔다. 권한 구멍은 아니었지만 **판단을 그르치게 하는 버그**였다.
원인은 어려운 로직이 아니라 **같은 규칙이 두 곳에 따로 적혀 있던 것**이다.

규칙이 코드에 흩어져 있으면 반드시 갈라진다. 그래서 규칙을 문서 하나에 모으고,
코드에는 그 규칙을 강제하는 **게이트 함수만** 둔다.

---

## 1. 용어

| 용어 | 정의 |
|---|---|
| **Principal** | 행동하는 주체. 사람(계정)이며, 역할 플래그의 조합으로 정의된다 |
| **Resource** | 보호 대상. 부서·제출물·양식·규칙·명단·감사로그 |
| **Action** | `read` · `write` · `delete` · `manage` 중 하나 |
| **Gate** | 판정을 내리는 **유일한** 함수. 전부 `src/server/authz.ts`에 산다 |
| **own / foreign** | 신원의 부서(own) 인가, 그 밖(foreign) 인가 |

---

## 2. Principal

Principal은 **네 가지**다. 조합해서 다섯 번째를 만들지 않는다.

| Principal | 우리말 | 판정식 | 무엇을 하는 사람인가 |
|---|---|---|---|
| `member` | **제출자** | 기본값 + `onRoster` | 자기 업무일지를 낸다 |
| `lead` | **부서담당자** | `divisionRole === 'lead'` | 부서 **문서**를 책임진다 — 열람·양식·규칙·병합 |
| `coordinator` | **총괄담당** | `isCoordinator` | 전사 총괄(기획조정실). 전 부서를 **읽고**, 부서 병합본을 모아 전사본을 만든다 |
| `operator` | **관리자** | `isOperator` | 시스템 소유자. 테넌시·인원 전권 + 전 부서 읽기 |

**역할은 겹칠 수 있다.** 운영자(Sean)는 동시에 자기 부서의 `lead`이자 `member`다.
한 사람이 여러 모자를 쓰는 것이지 새 Principal이 생기는 게 아니다 — 판정은 언제나
"이 행동에 필요한 권한을 가졌는가"이지 "이 사람이 누구인가"가 아니다.

| 사람 | member | lead | coordinator | operator |
|---|:---:|:---:|:---:|:---:|
| 부서원 | ✓ | | | |
| 부서담당자 | ✓ | ✓ | | |
| 김민하 (총괄) | ✓ | | ✓ | |
| Sean (운영자) | ✓ | ✓ | | ✓ |

파생 판정은 하나뿐이다:

```ts
readAll = isOperator || isCoordinator     // canReadAllDivisions()
```

**TACP-1 — Principal은 신원에서만 온다.**
URL·요청 본문·쿼리 파라미터가 Principal을 바꿀 수 없다. 세션(또는 Cloudflare Access JWT)이
누구인지 정하고, DB가 그 사람의 역할을 정한다. 그 사이에 사용자 입력이 끼어드는 경로는 없다.

**TACP-2 — 최고 권한은 API로 부여할 수 없다.**
`isOperator`·`isCoordinator`는 **어떤 API로도 변경되지 않는다.** 명단 API(`PUT /api/ops/roster`)가
받는 필드는 `onRoster` · `sortOrder` · `divisionRole` · `isActive` **넷뿐이다.**
운영자 권한을 늘리려면 DB에 직접 손대야 하고, 그건 서버 접근 권한이 있다는 뜻이다.
권한 상승 경로를 애플리케이션 안에 두지 않는다.

**TACP-3 — 문서는 부서가, 사람은 운영자가.**
`lead`는 양식·규칙·병합을 정하지만 **명단·역할은 못 바꾼다.**
`operator`는 명단·역할·부서 활성화를 정하지만 **남의 부서 문서를 고치지 않는다.**
두 권한을 한 Principal에 합치지 않는 이유는, 합치는 순간 "누가 이걸 바꿨나"가 흐려지기 때문이다.

---

## 3. Resource와 권한 행렬

`—` 는 권한 없음이며, **404로 표현된다** (TACP-5).

### 3.1 내 부서 (own)

| Resource | member | lead | coordinator | operator |
|---|:---:|:---:|:---:|:---:|
| 부서 페이지 · 제출 현황(이름·시각) | read | read | read | read |
| 내 제출물 | read/write | read/write | read/write | read/write |
| **내 제출물 삭제** | delete(마감 전) | delete(마감 전) | delete(마감 전) | delete |
| **남의 제출물 내용** | — | read | read | read |
| **남의 제출물 삭제** | — | — | — | delete |
| 부서 양식 내려받기 | read | read | read | read |
| 부서 양식 등록·교체 | — | write | write | write |
| 작성 안내 · 병합 규칙 | read | write | write | write |
| 병합 실행 | — | write | write | write |
| 명단 · 역할 · 부서 활성화 | — | — | — | manage |
| 감사 로그 | — | — | — | read |

### 3.2 병합본 (Resource: `MergeRun` · 결과 파일)

부서 병합본은 **제출물과 다른 자원이다.** 개인 문서가 아니라 부서가 대외로 내보내는
산출물이므로 공개 범위가 한 단계 넓다.

| Action | member | lead | coordinator | operator |
|---|:---:|:---:|:---:|:---:|
| 내 부서 병합본 존재·상태 보기 | read | read | read | read |
| 내 부서 병합본 **내려받기** | — | read | read | read |
| 병합 **실행·재실행** | — | write | write(자기 부서) | write(자기 부서) |
| **타 부서** 병합본 내려받기 | — | — | **read** | **read** |
| 전사 병합 실행 | — | — | **write** | **write** |

- member가 병합본을 못 받는 이유는 AU-06과 같다 — **남의 업무 내용**이 담겨 있다.
  현황(됐는지 안 됐는지)은 보여도 문서는 담당자부터다
- coordinator가 타 부서 **병합본**은 받되 **개별 제출물**은 받지 않는 게 원칙이다.
  총괄이 필요한 건 부서의 결론이지 개인의 원본이 아니다 (최소 권한)
- 타 부서 병합본 내려받기는 **감사 로그에 남는다** (TACP-10)

### 3.3 타 부서 (foreign)

| Resource | member | lead | coordinator | operator |
|---|:---:|:---:|:---:|:---:|
| **존재 여부조차** | — | — | read | read |
| 부서 페이지 · 제출 현황 | — | — | read | read |
| 제출물 내용 | — | — | read | read |
| **제출물 삭제** | — | — | — | **delete** |
| 양식 내려받기 | — | — | — | — |
| 양식 · 규칙 변경 | — | — | — | — |
| 제출 | — | — | — | — |
| 명단 · 역할 | — | — | — | manage |

**타 부서 열에서 `lead`·`coordinator`는 `read` 아니면 `—` 다.**
operator에게만 두 가지 예외가 있다 — 명단 관리(TACP-3, 문서가 아니라 사람에 대한 권한)와
제출물 삭제(TACP-14, 시스템 소유자의 정리 권한)다. 둘 다 감사 로그에 남는다.

---

## 4. 절대 불변식

바꿀 수 없다. 편의·일정·요청 어느 것도 근거가 되지 않는다.
바꿔야 한다면 그것은 **다른 제품**이며 §10을 거친다.

### TACP-4 — 격리가 기본값이다 (deny by default)

명시적으로 허용된 것만 허용된다. 새 기능은 **아무도 못 보는 상태로 태어나고**,
누가 볼 수 있는지 이 문서에 적은 뒤에야 열린다.

### TACP-5 — 권한 없음은 404다

403은 "있긴 있는데 넌 못 본다"를 알려준다. 그 자체가 정보 누출이다.
**없는 부서와 남의 부서는 구별되지 않아야 한다.**

| 상황 | 응답 |
|---|---|
| 미인증 | 401 → `/login` |
| 등록 안 된 사용자 · 비활성 계정 | 403 `not_registered` |
| 부서 미온보딩 | 403 `division_not_onboarded` |
| **권한 없는 리소스 · 없는 리소스** | **404 (구별 불가)** |

앞의 두 403은 **자기 자신에 대한 사실**이라 누출이 아니다. 리소스에 대한 판정은 전부 404다.

### TACP-6 — 쓰기 대상은 URL이 정하지 않는다

**쓰기는 언제나 신원의 부서에만 일어난다.** 업로드·양식 등록·규칙 변경 전부
`scope.division`을 대상으로 하며, URL 슬러그는 쓰기에 관여하지 않는다.

그래서 타 부서 페이지의 업로드 버튼은 눌려도 **내 부서에 저장된다.**
그런 버튼은 존재 자체가 함정이므로 **렌더하지 않는다** (TACP-9).

### TACP-7 — 읽기 대상은 단일 해석기를 통과한다

부서 스코프 데이터는 **반드시** `resolveTargetDivision(scope, slug?)`이 돌려준 부서로만 얻는다.

```
요청 없음 / 내 슬러그 / 내 별칭  → 내 부서 (isOwn = true)
타 부서                         → readAll만 + 감사 로그
그 외                           → 404
```

`scope.division`을 페이지·API에서 직접 쓰는 것은 **위반이다.** v1.3.0 사고가 정확히 이것이었다.
예외는 TACP-6의 쓰기 경로 하나뿐이며, 그때는 "신원의 부서"라는 의도를 주석으로 남긴다.

### TACP-8 — readAll은 읽기만이다 (예외: TACP-14)

`coordinator`·`operator`의 전 부서 권한은 **읽기 전용**이다.
`requireLead`가 `readAll`을 통과시키는 것은 lead 전용 **화면**을 보게 하기 위함이지,
남의 부서에 쓰라는 뜻이 아니다. 쓰기는 TACP-6이 구조적으로 막는다.

**유일한 예외는 operator의 제출물 삭제(TACP-14)다.**
`coordinator`는 해당되지 않는다 — readAll을 공유하지만 삭제는 못 한다.
즉 이 예외는 `readAll`에 붙은 것이 **아니라** `isOperator`에 붙은 것이다.
새 예외를 만들려면 §10을 거친다.

### TACP-9 — 화면은 권한을 반영한다

할 수 없는 행동의 버튼은 **비활성화가 아니라 렌더하지 않는다.**
비활성 버튼은 "권한만 있으면 되는 일"로 읽히고, 그건 대개 사실이 아니다.
타 부서를 열람 중이라는 사실은 **헤더 배지와 상단 배너로 항상 명시한다.**

### TACP-10 — 경계를 넘는 접근은 반드시 기록된다

`readAll`로 타 부서를 읽으면 `cross_division_read` 감사 로그가 남는다. 예외 없다.
기록되지 않는 열람 권한은 권한이 아니라 구멍이다.

### TACP-11 — 부서 안에서 현황은 공개, 내용은 lead부터

같은 부서원끼리는 **누가 냈는지·언제 냈는지** 서로 본다. 그래야 서로 챙긴다.
**파일 내용**은 lead부터다. 감시가 아니라 수합을 위한 최소 공개다.

### TACP-14 — 삭제는 본인과 운영자만, 마감이 본인을 막는다

삭제는 `read`·`write`와 **다른 Action이다.** 읽을 수 있다고 지울 수 있는 게 아니다.

| 주체 | 삭제 가능 | 조건 |
|---|:---:|---|
| 본인 | ✓ | **마감 전까지만** |
| operator | ✓ | 부서·마감 무관 |
| lead | ✗ | 남의 제출물을 **읽을 수는 있어도 지울 수는 없다** |
| coordinator | ✗ | 위와 같다 |

**lead를 뺀 이유**가 이 규칙의 핵심이다. lead는 부서 문서의 책임자지만(TACP-3),
제출물은 부서 문서가 아니라 **그 사람이 쓴 글**이다. 담당자가 부서원의 제출을 지울 수 있으면
"안 낸 것"과 "지워진 것"이 구별되지 않고, 그 순간 제출 현황은 사실의 기록이 아니게 된다.
담당자에게 필요한 것은 삭제가 아니라 **재제출 요청**이다.

**마감이 본인을 막는 이유.** 마감 후 삭제를 열어두면 병합본에는 들어갔는데
원본은 없는 상태가 만들어진다. 마감(WS-11)은 제출뿐 아니라 **철회에도 걸린다** — 예외 없다.

**operator가 마감 후에도 지울 수 있는 이유**는 §8과 같다. operator는 시스템 소유자이고
DB·서버에 직접 접근할 수 있으므로, UI로 막아봐야 능력이 줄지 않는다.
막는 대신 **감사 로그가 남는 경로**를 제공하는 편이 정직하다.

**삭제는 그 주차의 제출물 전체다.** v1만 지우고 v2를 남기는 부분 삭제는 없다.
버전은 "같은 제출의 이력"이지 별개 제출물이 아니므로, 일부만 지우면 이력이 거짓말을 한다.

**삭제는 되돌릴 수 없다.** 파일도 함께 지운다. 대신 감사 로그에
파일명·크기·sha256·버전 수를 남겨 **무엇이 있었는지는 사라지지 않게** 한다.

---

## 5. 게이트 — 판정이 사는 유일한 곳

**TACP-12 — 모든 게이트는 `src/server/authz.ts`에 있다.**
라우트·페이지·컴포넌트 안에서 역할 플래그를 직접 비교하는 것은 위반이다.
같은 판정이 두 곳에 적히는 순간 갈라지기 시작한다.

| 게이트 | 하는 일 | 실패 시 |
|---|---|---|
| `requireScope(headers)` | 신원 → 활성 사용자 + 부서 + 역할 | 401 / 403 |
| `requireLead(headers)` | lead 또는 readAll | **404** |
| `requireOperator(headers)` | operator | **404** |
| `resolveTargetDivision(scope, slug?)` | 대상 부서 해석 (TACP-7) | **404** |
| `findAccessibleSubmission(scope, id)` | 제출물 **읽기** 판정 | **404** |
| `requireDeletableSubmission(scope, id)` | 제출물 **삭제** 판정 (TACP-14) | **404** / 409 |
| `requireSubmitter(headers)` | 제출 진입점. 부서원이면 통과 (DM-16) | 401 / 403 |
| `getDivisionView(slug)` | 페이지용 — 위를 묶어 `{division, isOwn, canManage, canSubmit}` | 404 |

### 판정 순서

순서를 바꾸면 누출이 생긴다. 예컨대 리소스를 먼저 조회하면 **응답 시간 차이로 존재가 드러난다.**

```
1. 인증        — 누구인가              (verifyAccess)
2. 등록·활성   — 쓸 수 있는 사람인가    (requireScope)
3. 역할        — 이 화면을 볼 수 있나   (requireLead / requireOperator)
4. 대상 해석   — 어느 부서인가          (resolveTargetDivision)
5. 리소스 판정 — 이 건을 볼 수 있나     (findAccessibleSubmission)
6. 기록        — 경계를 넘었으면 감사    (audit)
```

---

## 6. 공개 엔드포인트

게이트를 통과하지 않는 경로는 **셋뿐이며, 늘리려면 §10을 거친다.**

| 경로 | 이유 |
|---|---|
| `POST /api/auth/login` | 로그인 자체. 사용자 열거 방지·8회 잠금은 별도 규칙 |
| `POST /api/auth/logout` | 세션 파기는 항상 가능해야 한다 |
| `GET /api/health` | 컨테이너 헬스체크. 개인정보·부서 정보를 담지 않는다 |

---

## 7. 릴리스 게이트 — 테스트

**TACP-13 — 격리 테스트가 실패하면 배포하지 않는다.** 예외 없다.

| ID | 검증 |
|---|---|
| AU-T12~17 | member·lead의 타 부서 접근 → 404 (슬러그·별칭 모두) |
| AU-T19 | 별칭으로 우회 시도 → 404 |
| AU-T20b | operator의 타 부서 해석 → `isOwn=false` + 감사 로그 |
| AU-T21b | 별칭 → 정식 슬러그 redirect |
| AU-T22b | zip은 요청한 부서의 파일만 담는다 |
| ST-15 | member의 같은 부서 타인 파일 → 404 |
| ST-T30 | 본인 삭제 → 허용. 마감 후 본인 삭제 → **409** |
| ST-T31 | **lead의 같은 부서 타인 제출물 삭제 → 404** (읽기는 되는데 삭제는 안 된다) |
| ST-T32 | coordinator의 타 부서 제출물 삭제 → 404 |
| ST-T33 | operator의 타 부서·마감 후 삭제 → 허용 + 감사 로그 |

새 Resource를 추가하면 **그 Resource의 격리 테스트를 같은 커밋에 넣는다.**

---

## 8. 명시적 비목표

지키지 **않는** 것을 적어두는 이유는, 나중에 "이것도 막혔겠지"라고 오해하지 않기 위함이다.

| 막지 않는 것 | 이유 |
|---|---|
| 부서원이 **화면을 캡처해** 밖으로 옮기는 것 | 열람 권한이 있는 사람의 재배포는 기술이 아니라 규정의 영역 |
| **사내망 도청** | 현재 평문 HTTP다 (AU-26). 사내망 신뢰를 전제로 한 잔여 위험 |
| 운영자 자신에 대한 방어 | operator는 시스템 소유자다. 감사 로그는 남지만 권한은 제한하지 않는다 |
| 제출 **내용**의 진위 | 누가 언제 냈는지는 보장하지만 내용이 사실인지는 판정하지 않는다 |

---

## 9. 위반 기록

새로 발견되면 여기에 추가한다. 지우지 않는다 — 같은 실수를 반복하지 않기 위한 기록이다.

| 일자 | 위반 | 어긴 규칙 | 조치 |
|---|---|---|---|
| 2026-08-14 | 레이아웃만 URL을 해석하고 페이지는 `scope.division` 사용 → 헤더/본문 부서 불일치 | TACP-7 | `resolveTargetDivision` 단일 출처 도입 (v1.3.1) |
| 2026-08-14 | operator 판정이 **4개 라우트에 각각 복사**되어 있었음 (`ops/divisions`·`ops/roster`·`ops/password-reset`·`template/standard`). 손으로 3개를 찾고, 네 번째는 TACP-12 테스트가 찾아냈다 | TACP-12 | `requireOperator` 게이트로 통합 + 재발 방지 테스트 (v1.3.2) |

---

## 10. 변경 절차

1. **이 문서를 먼저 고친다.** 어떤 규칙이 왜 바뀌는지, 무엇이 새로 허용/금지되는지
2. **불변식(§4)을 건드리는가?** 그렇다면 ADR을 새로 쓴다. 불변식은 조용히 바뀌지 않는다
3. **게이트를 고친다** — `authz.ts` 한 곳
4. **테스트를 추가한다** — 새로 허용된 것과 **새로 금지된 것 둘 다**
5. **화면을 맞춘다** (TACP-9)
6. 위반을 고친 것이면 §9에 기록한다

> 코드를 먼저 고치고 문서를 나중에 맞추는 것은 이 문서의 존재 이유를 없앤다.
