# 로드맵

> v2.1 (2026-08-13): **목표 확정 — 2026-08-17(월) 주차부터 파일럿 실사용** (Q-19).
> 오늘이 목 저녁이므로 실작업일은 금·토·일 3일. 스프린트로 쪼개고, 월요일에 꼭 필요한
> 것만 Sprint 1에 남긴다. 나머지는 **그 주 안에** 이어서 배포한다 (agile).

---

## 진행 현황 (v1.0.0, 2026-08-14)

| | 항목 | 상태 |
|---|---|---|
| ✅ | Sprint 1 전체 (S1-1~S1-12 상당) — 코어·화면·배포·실기동 | **완료** |
| ✅ | Sprint 2 조기 완료 — 드로어·설정 화면·/ops UI | **완료 (금요일에 앞당김)** |
| ✅ | 운영 컨테이너 가동 (`127.0.0.1:11111`) · 시드 · 백업 크론 | **완료** |
| 🔴 | Cloudflare 대시보드 (§3) + 브라우저 검증 (§5) — Sean만 가능 | **대기** |
| 🔴 | Q-01 한글 열기 확인 | **대기 (Phase 2 게이트)** |

---

## Sprint 1 — 월요일 오픈 최소셋 ✅ 완료 (8/13 목 밤 ~ 8/14 금)

> **기준: 월요일 아침에 부서원이 양식을 받고, 올리고, 화 14:00에 잠기고,
> Sean이 현황을 보고 내려받을 수 있다.** 이것만.

### 금 8/14 — 코어

| # | 작업 | 스펙 |
|---|---|---|
| S1-1 | Next.js+TS+Prisma 스캐폴드, 스키마, 시드 로더(`docs/private/seed.json`) | DM-* |
| S1-2 | `week.ts` — `mondayOf`·`describeWeek`·`deadlineFor`·`isLocked` + **테스트 (다중 TZ)** | WS-* |
| S1-3 | 인증·인가 — JWT 검증, `requireDivisionScope`, 역할 매트릭스, **격리 스위트** | AU-* |
| S1-4 | hwp 읽기 계층 — ole/record/model/reader + **왕복 동일성 게이트** (업로드 검증용) | HM-01~15 |

### 토 8/15 — 기능

| # | 작업 | 스펙 |
|---|---|---|
| S1-5 | 업로드 API — 검증 체인·원자적 쓰기·버전 트랜잭션 · `/api/me` · 다운로드 · 양식 | API ST |
| S1-6 | member 페이지 `/{slug}` — 업로드 4상태·카운트다운·양식받기·**부서 현황(축소판)** | PG CP |
| S1-7 | lead 화면 `/{slug}/manage` — 현황·개별받기·zip·미제출 복사 (**드로어 제외**) | PG-18,22~24 |
| S1-8 | 파일럿 데이터 — AI홍보전략실 활성화, 양식 시드(`fixtures/master-template.hwp`), 별칭 | DM-02,14 |

### 일 8/16 — 배포

| # | 작업 | 스펙 |
|---|---|---|
| S1-9 | Docker (`/data/worklog` 바인드, `127.0.0.1:11111`) · health(+루트 디스크 감시) | OPS |
| S1-10 | Cloudflare — Public Hostname `worklog` + Access 앱(`@kei.re.kr` 도메인 규칙) + AUD | AU-11 |
| S1-11 | 백업 크론(`/mnt/backup`) · 스모크 E2E · **AU-12 접근 검증 절차 실행** | OPS-07 AU-12 |
| S1-12 | Sean 본인 계정으로 실전 리허설 (업로드→잠금 시뮬→zip) | — |

### 월 8/17 — 오픈

- 아침에 부서원 안내 (별칭 링크 `/aiprd` 공유)
- **이번 주는 이메일 병행** (OPS-18) — 시스템 문제 시 업무는 계속 굴러간다
- Sean은 화 14:00 마감 후 zip으로 수합 → 기존 방식대로 병합·제출

### Sprint 1에서 뺀 것 (그 주 안에 후속 배포)

| 기능 | 이유 | 예정 |
|---|---|---|
| **FileDrawer (열람 드로어)** | 다운로드로 대체 가능. 수요일 병합 검토 전까지만 오면 됨 | Sprint 2 (화~수) |
| 설정 화면 (양식 교체 UI·규칙 에디터) | 양식은 시드로 이미 등록. 규칙은 Phase 2용 | Sprint 2 |
| `/ops` UI | 운영자가 Sean 본인 — 시드 스크립트로 대체 가능 | Sprint 2~3 |
| `/history` · 별칭 이외 편의 | 없어도 업무 가능 | Sprint 2 |
| coordinator `/overview` | 파일럿엔 부서가 1개 | Phase 3 |

---

## Sprint 2 — 보강 ✅ 조기 완료 (8/14 금) — 드로어·설정·/ops. 남은 항목만 아래 유지

| # | 작업 |
|---|---|
| S2-1 | **FileDrawer** — preview API + 표 렌더 + 버전 전환 (수요일 검토 전 목표) |
| S2-2 | 설정 화면 — 양식 교체(파싱 요약)·규칙 에디터(저장만)·onRoster 읽기 전용 안내 |
| S2-3 | `/ops` UI — 부서·인원·별칭 관리 (시드 스크립트 대체) |
| S2-4 | `/history` · 접근성 마무리 · 첫 주 피드백 반영 |
| S2-5 | 복구 리허설 1회 (OPS-09) · 운영 점검 |

**병행**: Q-01 — `fixtures/verify-write-test.hwp`를 한글에서 열어보기 (Phase 2 게이트, 30분)

---

## Phase 2 — 자동 병합 *(파일럿 안정 + 사용자 지시로 착수)*

| # | 작업 | 규모 |
|---|---|---|
| M11 | Q-01 확인 (미완이면 여기서) | 30분 |
| M12 | writer — 행 복제/삭제·표 삭제 (**`sample-filled-w2` 정답 대조**) | 2일 |
| M13 | 규칙 파서(Q-16 문법 확정)·병합·부분 실패·자체 점검 (HM-ABS) | 1.5일 |
| M14 | 병합 API·버튼 활성·경고 표시 | 0.5일 |
| M15 | 실데이터 병합 → 한글 육안 검증 → 수작업 대조 | 0.5일 |

## Phase 3 — 확산

부서 온보딩 절차화(부서당 ~30분) · coordinator `/overview` ·
복수 보고 부서(기획조정실) 보고 단위 분리 재검토 · 리마인드 재검토.
**확산 전에 파일럿 절감 효과를 숫자로** (기획조정실 협의 카드).

---

## 순서 요약

```
(지금) Round 3 확인
   ↓
금: 코어 4종★ → 토: 업로드+화면 → 일: 배포+리허설
   ↓
월 8/17 오픈 (이메일 병행) → 화 14:00 첫 실전 마감 → Sprint 2 보강 (드로어 등)
   ↓
[안정화] → Phase 2 (병합) → Phase 3 (확산)
```
