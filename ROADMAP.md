# 로드맵

> 순서에 의미가 있다. **각 단계 끝에서 시스템이 동작 가능한 상태**여야 한다.
> 스펙 ID로 작업을 추적한다.

---

## 착수 조건

| | 항목 | 상태 |
|---|---|---|
| 🔴 | [Q-02](OPEN-QUESTIONS.md) 8명 이름·이메일 | **대기 중 — 유일한 블로커** |
| ✅ | hwp 포맷 조사 | 완료 |
| ✅ | 스펙 작성 | 완료 |
| ✅ | 포트·터널 확인 | 완료 |

---

## Phase 1 — MVP

**완료 기준** (스펙 §4.6)

1. 8명이 각자 hwp를 업로드할 수 있다
2. 화요일 14:00 이후 업로드가 **실제로 막힌다**
3. 관리자가 현황 확인 + 개별/일괄 다운로드를 할 수 있다

이 3개만 되면 "화요일에 8통 보내고 하나씩 받는" 현재 방식은 **완전히 없어진다.**

---

### M1 — 기반 (약 0.5일)

| 작업 | 스펙 |
|---|---|
| Next.js + TypeScript + Tailwind 초기화 | — |
| Prisma 스키마 · 마이그레이션 | [DM-01~09](docs/spec/01-domain-model.md) |
| 8명 시드 | DM-02 ← **Q-02 필요** |
| 환경변수 검증 (zod) | OPS-06 |
| 구조적 로그 (pino) | OPS-11 |

**끝나면**: `npm run dev`로 뜨고 DB에 8명이 들어 있다.

---

### M2 — 주차 계산 (약 0.5일) ★

**여기부터 시작하는 것이 옳다.** 외부 의존이 없고, 나머지 전부가 여기에 기댄다.

| 작업 | 스펙 |
|---|---|
| `mondayOf` / `describeWeek` / `currentWeek` | [WS-01~05](docs/spec/02-week-slot.md) |
| `isLocked` / `msUntilDeadline` | WS-06 |
| 슬롯 지연 생성 `ensureCurrentSlot` | WS-11 |
| **테스트 19종 + 다중 TZ 실행** | WS-T01~T19 |

**끝나면**: 시간 관련 로직이 `TZ=UTC`에서도 전부 통과한다.

> 마감 로직은 이 시스템에서 **버그가 나면 가장 아픈 곳**이다.
> 화요일 14:00에 잘못 동작하면 그 주가 통째로 날아간다. 먼저, 확실하게 끝낸다.

---

### M3 — 인증 (약 0.5일)

| 작업 | 스펙 |
|---|---|
| JWT 검증 (`jose` + JWKS) | [AU-02](docs/spec/03-auth.md) |
| 개발 우회 + production 차단 | AU-03 |
| 사용자 조회 · 미등록 거부 | AU-04 |
| 관리자 판정 (404 은닉) | AU-06 |
| 테스트 10종 | AU-T01~T10 |

**끝나면**: `DEV_IDENTITY`로 로컬 개발이 가능하고, 위조 시나리오가 전부 막힌다.

---

### M4 — hwp 읽기 계층 (약 1일) ★

**Phase 2 엔진의 절반을 여기서 미리 만든다.** 업로드 검증에 필요하기 때문이다.

| 작업 | 스펙 |
|---|---|
| `ole.ts` — cfb 래핑 | [HM-02](docs/spec/08-hwp-merge-engine.md) |
| `record.ts` — 파싱/직렬화 | HM-04~08 |
| **왕복 바이트 동일성 테스트** | HM-T01 ← **필수 게이트** |
| `model.ts` — 표 파싱 | HM-09~12 |
| `reader.ts` — WorklogData 추출 | HM-15 |
| 테스트 (픽스처 3종) | HM-T02~T05 |

**끝나면**: 픽스처 3개의 왕복 동일성이 CI에서 초록이다.

> 조사 단계에서 파이썬으로 이미 검증한 로직이다. TypeScript로 옮기는 작업에 가깝다.
> `tools/`의 파이썬 구현이 **교차 검증 기준**이 된다.

---

### M5 — 업로드 (약 1일)

| 작업 | 스펙 |
|---|---|
| `POST /api/submissions` | [API-08~14](docs/spec/05-api.md) |
| 파일 검증 (크기·확장자·매직·구조) | [ST-04~09](docs/spec/04-storage.md) |
| 원자적 쓰기 (tmp → rename) | ST-10 |
| 버전 관리 트랜잭션 | DM-05 |
| `GET /api/me` | API-06~07 |
| 테스트 | ST-T01~T09, API-T01~T05 |

**끝나면**: curl로 업로드가 되고, 마감 후 409가 뜨고, 재업로드가 v2를 만든다.

---

### M6 — 업로드 화면 (약 1일)

| 작업 | 스펙 |
|---|---|
| 루트 레이아웃 · 헤더 | [PG-03~04](docs/spec/06-pages.md) |
| `<WeekBanner>` · `<DeadlineCountdown>` | CP-07~17 |
| `<UploadDropzone>` | CP-23~33 |
| `<SubmissionStatus>` · `<GuideNotice>` | CP-34~39, CP-21 |
| `/` 4가지 상태 | PG-05~16 |
| 컴포넌트 테스트 | CP-T01~T08, CP-T13 |

**끝나면**: 브라우저에서 실제로 파일을 올릴 수 있다. **8명에게 보여줄 수 있는 최초 시점.**

---

### M7 — 관리자 화면 (약 1일)

| 작업 | 스펙 |
|---|---|
| `GET /api/admin/status` | API-22~24 |
| `GET /api/admin/download-zip` | API-25~29, ST-16 |
| `GET /api/admin/slots` | API-30 |
| `<StatusSummary>` · `<SubmissionTable>` | CP-44~53 |
| `<SlotSelector>` · `<BulkActions>` | CP-40~43, CP-58~61 |
| `<CopyMissingButton>` | CP-62~65 |
| `/admin`, `/admin/[isoKey]` | PG-18~28 |

**끝나면**: **Phase 1 완료 기준 3개 충족.**

---

### M8 — 부가 화면 (약 0.5일)

| 작업 | 스펙 |
|---|---|
| `GET /api/template` | API-19~21 |
| `/history` | PG-17 |
| `error.tsx` · `not-found.tsx` · `loading.tsx` | PG-29~31 |
| 반응형 · 접근성 | PG-32~37 |

---

### M9 — 배포 (약 1일)

| 작업 | 스펙 |
|---|---|
| Dockerfile · compose | [OPS-04](docs/spec/09-deployment-ops.md) |
| **`127.0.0.1:11111` 바인딩 확인** | OPS-03 ← 리뷰 필수 |
| `/api/health` | API-31~33 |
| Cloudflare Public Hostname + Access 앱 | AU-11 ← **Q-04** |
| 백업 스크립트 · 크론 | OPS-07~08 |
| 접근 검증 절차 | AU-12 |
| E2E (Playwright) | PG-T01~T10 |

**끝나면**: `https://worklog.excusa.uk` 접속 가능. 사내망 직접 접근은 차단.

---

### M10 — 시범 운영 (1~2주)

| 작업 |
|---|
| 1명으로 Access 로그인 선검증 ([Q-03](OPEN-QUESTIONS.md)) |
| 8명에게 안내 |
| **첫 1~2주는 이메일 병행** ([Q-09](OPEN-QUESTIONS.md)) |
| 로그 관찰 · 피드백 수집 |
| 복구 리허설 1회 (OPS-09) |

> 병행 기간을 두는 이유: 실패해도 그 주 업무가 안 멈춘다.
> 자신 있어도 첫 주는 병행한다.

---

## Phase 1 합계

**약 7~8일** (시범 운영 제외) · 순수 작업 기준

---

## Phase 2 — 자동 병합

> **착수는 사용자의 명시적 지시가 있을 때.** Phase 1 안정화 후.

### M11 — 사전 확인 ★ (30분)

| 작업 |
|---|
| **[Q-01](OPEN-QUESTIONS.md) — `fixtures/verify-write-test.hwp`를 한글에서 열어 보기** |

> **가장 먼저, 반드시.** 여기서 실패하면 이후 설계가 전부 무의미하다.
> 30분짜리 확인이 며칠을 아낀다. 실패 시 [ADR-0001](docs/adr/0001-hwp5-binary-record-engine.md) 되돌리고 `.hwpx` 전환.

### M12 — 쓰기 계층 (약 2일)

| 작업 | 스펙 |
|---|---|
| 행 복제/삭제 (rowAddr·nRows·rowSize 정합) | HM-03, HM-16 |
| 텍스트 치환 (nChars 플래그 보존) | HM-10~11 |
| 표 전체 삭제 | HM-16d |
| **`sample-filled-w2`와 구조 대조** | HM-24, HM-T10 |
| 테스트 | HM-T06~T10, HM-T14~T16 |

### M13 — 병합 정책 (약 1일)

| 작업 | 스펙 |
|---|---|
| 팀 순 정렬 | HM-18 ← **Q-02 팀 배정 필요** |
| 병합 절차 · 구분 재생성 | HM-19 |
| 부분 실패 허용 + 경고 | HM-21 |
| 자체 점검 (유실 방지) | HM-22 |
| 테스트 | HM-T11~T13, HM-T17 |

### M14 — 병합 UI (약 0.5일)

| 작업 | 스펙 |
|---|---|
| `POST /api/admin/merge` 실동작 | API-34~36 |
| `자동 병합` 버튼 활성화 | CP-60 |
| 진행 상태 · 경고 표시 | — |

### M15 — 검증 (약 0.5일)

| 작업 | 스펙 |
|---|---|
| 실제 8명 데이터로 병합 | — |
| **한글 육안 검증 체크리스트** | HM-25 |
| 수작업 결과와 비교 | — |

**Phase 2 합계: 약 4일**

---

## Phase 3 — 리마인드 (선택)

> **착수 전에 [Q-10](OPEN-QUESTIONS.md)을 먼저 판단한다.**

관리자 화면의 `미제출 N명 복사` 버튼(PG-27)이 이미 효용의 대부분을 낸다.
Phase 1을 몇 주 운영해 본 뒤, 자동 알림이 정말 필요한지 확인하고 결정한다.

필요하다고 판단되면:

| 작업 |
|---|
| 알림 채널 확인 (사내 메신저 웹훅? 이메일?) |
| 화요일 11:00 미제출자 조회 |
| 발송 · 실패 처리 |

---

## 작업 순서 요약

```
Q-02 답변
   ↓
M1 기반 → M2 주차계산★ → M3 인증 → M4 hwp읽기★
   ↓
M5 업로드 API → M6 업로드 화면 → M7 관리자 화면
   ↓                                    ↓
M8 부가          ────────────► Phase 1 완료 기준 충족
   ↓
M9 배포 → M10 시범운영(이메일 병행)
   ↓
[Phase 1 안정화 · 사용자 지시 대기]
   ↓
M11 Q-01 확인★ → M12 쓰기 → M13 병합 → M14 UI → M15 검증
```

★ = 위험이 집중된 구간. 여기서 시간을 아끼지 않는다.
